# Local Docker Desktop — default for development
# Start DB: docker compose up -d
DATABASE_URL="postgresql://admin:password123@localhost:5432/hydraflow"
DIRECT_URL="postgresql://admin:password123@localhost:5432/hydraflow"

# Production (Neon) goes in .env.production — never commit real secrets
# DATABASE_URL="postgresql://USER:PASSWORD@HOST-pooler.REGION.aws.neon.tech/DB?sslmode=require"
# DIRECT_URL="postgresql://USER:PASSWORD@HOST.REGION.aws.neon.tech/DB?sslmode=require"

PORT=3000
JWT_SECRET="change-me"
GOOGLE_CLIENT_ID=
ALLOW_TEST_LOGIN=true
